 <nav>
    <div class="nav-wrapper">
        <img id="logo" src="/assets/logo.png">
    </div>
  </nav>
<div class="row">
    {{#each model as |project|}}
        <div class="col s4">
            {{#project-card proj=project}}
            {{/project-card}}
        </div>
    {{/each}}
</div>


{{outlet}}